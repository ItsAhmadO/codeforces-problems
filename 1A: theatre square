#include <iostream>
#include <cmath>
#define ll long long
using namespace std;
 
int main(){
    ll n,m,a;
    cin >> n >> m >> a;
    n = ceil(n/a) + (n%a != 0);
    m = ceil(m/a) + (m%a != 0);
    cout << n*m;
    return 0;
}
