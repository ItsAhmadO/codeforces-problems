#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    int n,a[110];
    cin >> n;
    double ans, sum = 0;
    for(int i = 0; i<n; i++){
        cin >> a[i];
        sum += a[i];
    }
    ans = sum/n;
    cout << ans;
    return 0;
}
